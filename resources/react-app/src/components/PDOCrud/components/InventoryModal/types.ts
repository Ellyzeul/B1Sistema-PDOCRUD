export type InventoryModalProps = {
    isbn: string
}

export type BookData = {
    isbn: string,
    quantity: number,
    condition: string,
    location: string,
    bookshelf: number,
    obsertation: string
} 