export type ZipCodeConsultationProp = {
  bling_postal_code: string,
  country: string, 
}

export type AdressData = {
  postal_code: string,
  adress: string,
  county: string,
  city: string,
  uf: string,
}